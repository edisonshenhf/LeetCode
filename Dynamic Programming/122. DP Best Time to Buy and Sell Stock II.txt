class Solution {
    public int maxProfit(int[] prices) {
        int res= 0;
        int n = prices.length;
        if(n<2) return 0;
        for(int i=1;i<n;i++){
            res+=Math.max(prices[i]-prices[i-1],0);
        }
        return res;
    }
}